<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.faiz.udacitytask4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity"

            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.FamilyActivity"
            android:label="Family"
            />
        <activity android:name=".Activities.PhrasesActivity"
            android:label="Phrases"
            />
        <activity android:name=".Activities.NumberActivity"
            android:label="Number"
            />
        <activity android:name=".Activities.ColorActivity"
            android:label="Color"
            ></activity>
    </application>

</manifest>